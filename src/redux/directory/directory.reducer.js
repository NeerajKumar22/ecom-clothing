import INITIAL_STATE_DATA from "./directory.data";

const directoryReducer = (state = INITIAL_STATE_DATA, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default directoryReducer;
